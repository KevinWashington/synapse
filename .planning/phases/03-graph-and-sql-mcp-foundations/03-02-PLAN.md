---
phase: 03-graph-and-sql-mcp-foundations
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/postgres_mcp_service.py
  - backend/app/database.py
  - backend/app/config.py
autonomous: true
requirements:
  - R3
user_setup: []
must_haves:
  truths:
    - PostgreSQL MCP server supports constrained read and write operations.
    - SQL health check validates DB connectivity and extension readiness.
  artifacts:
    - postgres MCP service exists with query/write/health method wrappers.
    - database helpers expose safe execution path for MCP SQL methods.
  key_links:
    - host can register sql.query, sql.write, and sql.health against postgres service.
---

<objective>
Create PostgreSQL MCP service foundation with guarded read/write behavior.

Purpose: satisfy SQL-side R3 foundation and enable host-managed SQL operations.
Output: MCP-style postgres adapter and connectivity/health checks.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-graph-and-sql-mcp-foundations/RESEARCH.md
@backend/app/database.py
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postgres MCP service</name>
  <files>backend/app/services/postgres_mcp_service.py</files>
  <read_first>backend/app/database.py</read_first>
  <action>Create service methods for sql.query, sql.write, and sql.health using async session execution and normalized result shape.</action>
  <verify><automated>python -m compileall backend/app/services/postgres_mcp_service.py</automated></verify>
  <acceptance_criteria>
    - service exposes sql query/write/health methods
    - method responses include row counts or status metadata
  </acceptance_criteria>
  <done>SQL MCP adapter exists with baseline read/write/health support.</done>
</task>

<task type="auto">
  <name>Task 2: Add SQL method safety constraints</name>
  <files>backend/app/services/postgres_mcp_service.py, backend/app/config.py</files>
  <read_first>backend/app/config.py, backend/app/services/postgres_mcp_service.py</read_first>
  <action>Add explicit guardrails for writable statements and configurable limits for query rows/timeouts to reduce unsafe execution risk.</action>
  <verify><automated>python -m compileall backend/app/services/postgres_mcp_service.py backend/app/config.py</automated></verify>
  <acceptance_criteria>
    - config includes SQL MCP safeguards
    - service rejects disallowed statement classes
  </acceptance_criteria>
  <done>SQL adapter applies clear safety controls.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure DB health helper supports MCP checks</name>
  <files>backend/app/database.py</files>
  <read_first>backend/app/database.py</read_first>
  <action>Add lightweight health helper used by sql.health to confirm connectivity and key extension availability.</action>
  <verify><automated>python -m compileall backend/app/database.py</automated></verify>
  <acceptance_criteria>
    - database module exposes a health helper for MCP SQL checks
  </acceptance_criteria>
  <done>PostgreSQL MCP health operation has a reusable DB-level implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SQL adapter compiles and exposes query/write/health methods
- [ ] Safety controls are configurable and enforced
- [ ] DB health helper returns structured status
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Baseline SQL MCP foundation is operationally defined
  </success_criteria>

<output>
After completion, create `.planning/phases/03-graph-and-sql-mcp-foundations/03-02-SUMMARY.md`
</output>

---
phase: 03-graph-and-sql-mcp-foundations
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - backend/app/main.py
  - backend/app/routers/stats.py
  - backend/app/core/dependencies.py
autonomous: true
requirements:
  - R3
user_setup: []
must_haves:
  truths:
    - Host startup registers graph and sql MCP capabilities.
    - Diagnostics endpoint reports graph and sql health status.
  artifacts:
    - main startup wiring includes graph/sql registration and health checks.
    - stats diagnostics endpoint includes graph/sql MCP health section.
  key_links:
    - stats diagnostics read host capability mapping and service health outputs.
---

<objective>
Wire graph and SQL MCP services into app startup and diagnostics surface.

Purpose: make R3 capabilities visible and operational in runtime.
Output: registration wiring and API diagnostics for graph/sql MCP health.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/main.py
@backend/app/routers/stats.py
@backend/app/core/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add startup registration for graph and SQL MCP services</name>
  <files>backend/app/main.py, backend/app/core/dependencies.py</files>
  <read_first>backend/app/main.py, backend/app/core/dependencies.py</read_first>
  <action>Wire graph and SQL service providers into startup flow so host registration and dependency access are explicit and stable.</action>
  <verify><automated>python -m compileall backend/app/main.py backend/app/core/dependencies.py</automated></verify>
  <acceptance_criteria>
    - startup path initializes graph and sql MCP providers
    - dependency layer exposes providers used by diagnostics
  </acceptance_criteria>
  <done>Runtime wiring for graph/sql MCP services is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Extend stats diagnostics with graph/sql health</name>
  <files>backend/app/routers/stats.py</files>
  <read_first>backend/app/routers/stats.py</read_first>
  <action>Add diagnostics fields and/or endpoint output covering graph and sql MCP health checks and last-error summaries.</action>
  <verify><automated>python -m compileall backend/app/routers/stats.py</automated></verify>
  <acceptance_criteria>
    - stats diagnostics include graph health and sql health status
  </acceptance_criteria>
  <done>Operational diagnostics expose graph/sql MCP readiness.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Startup registration includes graph and sql method capabilities
- [ ] Stats diagnostics returns graph and sql health data
- [ ] No startup regressions are introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- R3 runtime diagnostics are visible through API
  </success_criteria>

<output>
After completion, create `.planning/phases/03-graph-and-sql-mcp-foundations/03-03-SUMMARY.md`
</output>

---
phase: 03-graph-and-sql-mcp-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/neo4j_service.py
  - backend/app/schemas/ai.py
  - backend/app/services/mcp_host_service.py
autonomous: true
requirements:
  - R3
user_setup: []
must_haves:
  truths:
    - Neo4j MCP server supports baseline read and write methods.
    - Graph operations expose explicit health status for diagnostics.
  artifacts:
    - neo4j_service contains MCP-style read/write method wrappers.
    - schema layer includes request/response payload contracts for graph operations.
  key_links:
    - host service can route graph.query and graph.write methods to neo4j adapter.
---

<objective>
Create baseline Neo4j MCP contract and method surface for graph operations.

Purpose: satisfy graph-side R3 foundation with explicit method boundaries.
Output: MCP-style graph adapter methods plus schema contracts and host routing readiness.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-graph-and-sql-mcp-foundations/CONTEXT.md
@backend/app/services/neo4j_service.py
@backend/app/services/mcp_host_service.py
@backend/app/schemas/ai.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Neo4j MCP request and response contracts</name>
  <files>backend/app/schemas/ai.py</files>
  <read_first>backend/app/schemas/ai.py</read_first>
  <action>Add graph operation payload schemas for query, write, and health methods with strict field definitions and operation metadata.</action>
  <verify><automated>python -m compileall backend/app/schemas/ai.py</automated></verify>
  <acceptance_criteria>
    - schema file defines graph query/write payload models
    - health response payload model exists
  </acceptance_criteria>
  <done>Graph MCP payload contracts are explicit and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Neo4j MCP read/write wrappers</name>
  <files>backend/app/services/neo4j_service.py</files>
  <read_first>backend/app/services/neo4j_service.py</read_first>
  <action>Add wrapper methods aligned to MCP method names (`graph.query`, `graph.write`, `graph.health`) with structured success/error output.</action>
  <verify><automated>python -m compileall backend/app/services/neo4j_service.py</automated></verify>
  <acceptance_criteria>
    - service exposes graph.query and graph.write wrapper methods
    - graph.health returns driver connectivity status
  </acceptance_criteria>
  <done>Neo4j service supports baseline MCP read/write/health flows.</done>
</task>

<task type="auto">
  <name>Task 3: Update host routing map for graph methods</name>
  <files>backend/app/services/mcp_host_service.py</files>
  <read_first>backend/app/services/mcp_host_service.py</read_first>
  <action>Ensure host dispatch path can route and report graph method calls with clear method-to-server diagnostics.</action>
  <verify><automated>python -m compileall backend/app/services/mcp_host_service.py</automated></verify>
  <acceptance_criteria>
    - host diagnostics show graph capability mapping
  </acceptance_criteria>
  <done>Host is graph-method aware for operational diagnostics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Graph method wrappers compile and are discoverable in service layer
- [ ] Host diagnostics expose graph capability routing
- [ ] No import/runtime errors introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Baseline graph MCP foundation is operationally defined
  </success_criteria>

<output>
After completion, create `.planning/phases/03-graph-and-sql-mcp-foundations/03-01-SUMMARY.md`
</output>

---
phase: 01-mcp-host-and-contracts
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/app/routers/stats.py
  - backend/app/services/mcp_host_service.py
  - readme.md
autonomous: true
requirements:
  - R1
user_setup: []
must_haves:
  truths:
    - Host exposes operational diagnostics for registered MCP servers.
    - Failures include clear source, reason, and remediation hints.
  artifacts:
    - Diagnostic endpoint or stats integration reflects host/server status.
    - Documentation describes MCP host contracts and operational checks.
  key_links:
    - stats or diagnostics endpoint reads host state from service layer.
---

<objective>
Add observability and operational checks for MCP host/server orchestration.

Purpose: make failures diagnosable and reduce integration ambiguity.
Output: runtime diagnostics plus documentation for operators and developers.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-host-and-contracts/01-02-PLAN.md
@backend/app/routers/stats.py
@backend/app/services/mcp_host_service.py
@readme.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add host diagnostics surface</name>
  <files>backend/app/routers/stats.py, backend/app/services/mcp_host_service.py</files>
  <read_first>backend/app/routers/stats.py, backend/app/services/mcp_host_service.py</read_first>
  <action>Expose registered server count, capability map, health snapshot, and last known errors through an existing or new stats endpoint.</action>
  <verify>rg -n "mcp|host|health|diagnostic|server" backend/app/routers/stats.py backend/app/services/mcp_host_service.py</verify>
  <acceptance_criteria>
    - diagnostics endpoint includes host/server status fields
    - degraded servers are visible without inspecting raw logs
  </acceptance_criteria>
  <done>Operational diagnostics are queryable from API surface.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize host error reporting</name>
  <files>backend/app/services/mcp_host_service.py</files>
  <read_first>backend/app/services/mcp_host_service.py</read_first>
  <action>Standardize error categories and include remediation hints for timeout, protocol mismatch, unreachable server, and validation errors.</action>
  <verify>rg -n "timeout|protocol|unreachable|validation|hint" backend/app/services/mcp_host_service.py</verify>
  <acceptance_criteria>
    - error payloads include category and actionable hint fields
  </acceptance_criteria>
  <done>Host errors are consistently classified and actionable.</done>
</task>

<task type="auto">
  <name>Task 3: Document MCP host operations</name>
  <files>readme.md</files>
  <read_first>readme.md</read_first>
  <action>Document contract assumptions, startup expectations, diagnostics endpoint usage, and common failure recovery steps.</action>
  <verify>rg -n "MCP|host|diagnostic|JSON-RPC|recovery" readme.md</verify>
  <acceptance_criteria>
    - readme includes actionable MCP host operation section
  </acceptance_criteria>
  <done>Operational docs support onboarding and troubleshooting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Diagnostics endpoint returns host/server status shape
- [ ] rg checks for categorized error fields and docs section pass
- [ ] No regression in existing stats routes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Host operations are observable and diagnosable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-host-and-contracts/01-03-SUMMARY.md`
</output>

---
phase: 01-mcp-host-and-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/ai.py
  - backend/app/services/ai_service.py
  - backend/app/config.py
autonomous: true
requirements:
  - R1
user_setup: []
must_haves:
  truths:
    - MCP request and response envelopes are explicitly modeled in backend code.
    - Contract validation failures return structured protocol errors.
  artifacts:
    - backend/app/schemas/ai.py contains MCP envelope schemas.
    - backend/app/config.py contains host contract configuration values.
  key_links:
    - ai_service uses contract schemas for validation.
---

<objective>
Define explicit MCP host/server contracts for JSON-RPC messaging and error handling.

Purpose: prevent implicit, fragile cross-service assumptions.
Output: validated schema and config foundation for host orchestration.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-mcp-host-and-contracts/CONTEXT.md
@backend/app/schemas/ai.py
@backend/app/services/ai_service.py
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON-RPC envelope schemas</name>
  <files>backend/app/schemas/ai.py</files>
  <read_first>backend/app/schemas/ai.py, backend/app/routers/ai.py</read_first>
  <action>Add request/response/error schema models for MCP-style JSON-RPC envelopes including id, method, params, result, and structured error fields.</action>
  <verify>rg -n "jsonrpc|method|params|result|error" backend/app/schemas/ai.py</verify>
  <acceptance_criteria>
    - backend/app/schemas/ai.py contains explicit JSON-RPC envelope models
    - error model includes code and message fields
  </acceptance_criteria>
  <done>Contract models exist and are importable by service layer.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce host contract config defaults</name>
  <files>backend/app/config.py</files>
  <read_first>backend/app/config.py</read_first>
  <action>Add configuration values for host timeout, retry policy, and protocol version defaults used by orchestration paths.</action>
  <verify>rg -n "timeout|retry|jsonrpc|mcp" backend/app/config.py</verify>
  <acceptance_criteria>
    - backend/app/config.py defines timeout and retry configuration for MCP orchestration
  </acceptance_criteria>
  <done>Host contract config values are centralized and documented in code.</done>
</task>

<task type="auto">
  <name>Task 3: Wire contract validation in AI service boundary</name>
  <files>backend/app/services/ai_service.py</files>
  <read_first>backend/app/services/ai_service.py, backend/app/schemas/ai.py</read_first>
  <action>Use new schema models to validate inbound/outbound payload envelopes at service boundary before external tool invocation.</action>
  <verify>rg -n "MCP|jsonrpc|validate|schema" backend/app/services/ai_service.py</verify>
  <acceptance_criteria>
    - ai_service references new contract schemas
    - invalid envelope path maps to structured error response
  </acceptance_criteria>
  <done>Service boundary enforces contract shape for MCP interactions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] rg -n "jsonrpc|MCP" backend/app/schemas/ai.py backend/app/services/ai_service.py backend/app/config.py
- [ ] Existing backend tests or smoke checks still pass
- [ ] Structured errors are returned for malformed envelopes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Host contract definitions are explicit and reusable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-host-and-contracts/01-01-SUMMARY.md`
</output>

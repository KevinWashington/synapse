---
phase: 01-mcp-host-and-contracts
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/app/services/mcp_host_service.py
  - backend/app/main.py
  - backend/app/core/dependencies.py
autonomous: true
requirements:
  - R1
user_setup: []
must_haves:
  truths:
    - Host orchestrator can register MCP servers and dispatch requests by capability.
    - Timeout and retry policy are applied consistently.
  artifacts:
    - backend/app/services/mcp_host_service.py implements registration and dispatch.
    - backend/app/main.py initializes host service at app startup.
  key_links:
    - dependencies expose host service to routers/services.
---

<objective>
Implement MCP host orchestration service and dependency wiring.

Purpose: establish a reusable execution path for all MCP server calls.
Output: central host service with registration, dispatch, and policy enforcement.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-host-and-contracts/01-01-PLAN.md
@backend/app/main.py
@backend/app/core/dependencies.py
@backend/app/services
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create host orchestration service</name>
  <files>backend/app/services/mcp_host_service.py</files>
  <read_first>backend/app/services/ai_service.py, backend/app/services/rag_service.py</read_first>
  <action>Create a host service that supports server registration, capability lookup, request dispatch, timeout handling, and structured error mapping.</action>
  <verify>rg -n "class|register|dispatch|timeout|retry" backend/app/services/mcp_host_service.py</verify>
  <acceptance_criteria>
    - service has explicit register and dispatch methods
    - timeout and retry are configurable and enforced
  </acceptance_criteria>
  <done>Host orchestration logic exists and is reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire host lifecycle into app startup</name>
  <files>backend/app/main.py</files>
  <read_first>backend/app/main.py</read_first>
  <action>Initialize and register baseline local MCP servers during application startup with clear startup logging.</action>
  <verify>rg -n "startup|mcp|host|register" backend/app/main.py</verify>
  <acceptance_criteria>
    - startup path initializes host service
    - baseline server registration path is present
  </acceptance_criteria>
  <done>Application lifecycle includes host initialization.</done>
</task>

<task type="auto">
  <name>Task 3: Expose host via dependency layer</name>
  <files>backend/app/core/dependencies.py</files>
  <read_first>backend/app/core/dependencies.py</read_first>
  <action>Add dependency provider(s) that make host service injectable in routers/services without global import coupling.</action>
  <verify>rg -n "mcp|host|Depends|dependency" backend/app/core/dependencies.py</verify>
  <acceptance_criteria>
    - dependency layer can provide host service instance
  </acceptance_criteria>
  <done>Consumers can access host service through dependency injection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] rg -n "register|dispatch|timeout|retry" backend/app/services/mcp_host_service.py
- [ ] Application starts without import/runtime errors
- [ ] Host dependency is resolvable in at least one consumer path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Host orchestration is centralized and reusable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-host-and-contracts/01-02-SUMMARY.md`
</output>

---
phase: 05-traceability-and-validation
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/app/routers/ai.py
  - backend/app/routers/stats.py
  - backend/app/services/mcp_host_service.py
autonomous: true
requirements:
  - R5
  - R6
user_setup: []
must_haves:
  truths:
    - Project chat API returns provenance alongside generated content and sources.
    - Diagnostics expose provenance coverage and isolation indicators for auditing.
  artifacts:
    - ai router maps RAG provenance fields into response payload.
    - stats endpoints include provenance/isolation audit views.
  key_links:
    - diagnostics correlate isolation counters with traceability data to support audits.
---

<objective>
Wire provenance and audit data into API response and diagnostics surfaces.

Purpose: make traceability observable to clients and operations.
Output: enriched project-chat responses and diagnostics endpoints for provenance/isolation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-traceability-and-validation/05-01-PLAN.md
@backend/app/routers/ai.py
@backend/app/routers/stats.py
@backend/app/services/mcp_host_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return provenance in project-chat response</name>
  <files>backend/app/routers/ai.py</files>
  <read_first>backend/app/routers/ai.py, backend/app/schemas/ai.py</read_first>
  <action>Map retrieval provenance metadata into project-chat response payload, preserving existing source compatibility while adding structured traceability fields.</action>
  <verify><automated>python -m compileall backend/app/routers/ai.py</automated></verify>
  <acceptance_criteria>
    - project-chat response includes provenance section/fields
    - existing source list remains backward compatible
  </acceptance_criteria>
  <done>Client-facing chat API exposes source provenance metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add provenance and isolation diagnostics endpoint</name>
  <files>backend/app/routers/stats.py</files>
  <read_first>backend/app/routers/stats.py</read_first>
  <action>Add diagnostics route returning provenance coverage metrics and isolation counters using existing host/retrieval/anchor diagnostics services.</action>
  <verify><automated>python -m compileall backend/app/routers/stats.py</automated></verify>
  <acceptance_criteria>
    - stats diagnostics include provenance and isolation indicators
  </acceptance_criteria>
  <done>Operations can query traceability/isolation audit metrics from API.</done>
</task>

<task type="auto">
  <name>Task 3: Extend host diagnostics for audit correlation</name>
  <files>backend/app/services/mcp_host_service.py</files>
  <read_first>backend/app/services/mcp_host_service.py</read_first>
  <action>Add minimal audit fields/counters to host diagnostics payload to support cross-system traceability correlation in stats endpoints.</action>
  <verify><automated>python -m compileall backend/app/services/mcp_host_service.py</automated></verify>
  <acceptance_criteria>
    - host diagnostics include fields consumed by provenance/isolation audit endpoint
  </acceptance_criteria>
  <done>Host diagnostics can be joined with provenance/isolation audit views.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] project-chat response contains provenance metadata
- [ ] stats diagnostics expose provenance and isolation metrics
- [ ] host diagnostics include audit correlation fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Traceability and isolation are observable through API responses/diagnostics
  </success_criteria>

<output>
After completion, create `.planning/phases/05-traceability-and-validation/05-02-SUMMARY.md`
</output>

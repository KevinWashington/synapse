---
phase: 05-traceability-and-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/ai.py
  - backend/app/services/rag_service.py
  - backend/app/services/qdrant_retrieval_service.py
autonomous: true
requirements:
  - R5
user_setup: []
must_haves:
  truths:
    - Retrieval outputs carry explicit provenance fields identifying contributing subsystem(s).
    - Source-level metadata includes canonical anchors needed for auditing.
  artifacts:
    - AI schema contracts include provenance models and response fields.
    - RAG service emits provenance metadata per retrieved source.
  key_links:
    - RAG output fields map directly into API response schema without ad-hoc transformation.
---

<objective>
Define and emit provenance contracts from retrieval layer.

Purpose: establish machine-readable traceability for all downstream chat responses.
Output: schema + retrieval provenance metadata aligned with runtime flow.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-traceability-and-validation/CONTEXT.md
@backend/app/schemas/ai.py
@backend/app/services/rag_service.py
@backend/app/services/qdrant_retrieval_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provenance response models to AI schema</name>
  <files>backend/app/schemas/ai.py</files>
  <read_first>backend/app/schemas/ai.py</read_first>
  <action>Add explicit provenance models/fields for source subsystem, anchor key, and retrieval metadata in project-chat response contracts.</action>
  <verify><automated>python -m compileall backend/app/schemas/ai.py</automated></verify>
  <acceptance_criteria>
    - schema includes provenance model(s)
    - project chat response supports provenance payload
  </acceptance_criteria>
  <done>API contract supports structured provenance output.</done>
</task>

<task type="auto">
  <name>Task 2: Emit provenance metadata in RAG retrieval results</name>
  <files>backend/app/services/rag_service.py</files>
  <read_first>backend/app/services/rag_service.py</read_first>
  <action>Ensure each retrieved article contains provenance metadata indicating retrieval backend/subsystem and anchor identifiers (paper_id/project_id).</action>
  <verify><automated>python -m compileall backend/app/services/rag_service.py</automated></verify>
  <acceptance_criteria>
    - retrieved metadata includes provenance subsystem and anchor fields
  </acceptance_criteria>
  <done>RAG layer consistently emits traceability metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Align qdrant adapter provenance shape</name>
  <files>backend/app/services/qdrant_retrieval_service.py</files>
  <read_first>backend/app/services/qdrant_retrieval_service.py</read_first>
  <action>Expose adapter-side provenance markers (vector source + payload field guarantees) so qdrant path matches schema expectations.</action>
  <verify><automated>python -m compileall backend/app/services/qdrant_retrieval_service.py</automated></verify>
  <acceptance_criteria>
    - qdrant adapter result contract includes provenance markers
  </acceptance_criteria>
  <done>Vector retrieval path is provenance-compatible with schema contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Provenance models compile and are referenced by response contracts
- [ ] Retrieval records include subsystem + anchor metadata
- [ ] Vector adapter provenance shape matches retrieval contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Provenance contract is explicit at schema and retrieval boundaries
  </success_criteria>

<output>
After completion, create `.planning/phases/05-traceability-and-validation/05-01-SUMMARY.md`
</output>

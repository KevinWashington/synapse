---
phase: 05-traceability-and-validation
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - backend/tests/test_traceability_contract.py
  - backend/tests/test_isolation_guards.py
  - backend/tests/conftest.py
autonomous: true
requirements:
  - R5
  - R6
user_setup: []
must_haves:
  truths:
    - Automated tests fail if provenance fields are removed from responses.
    - Automated tests fail if project isolation checks are bypassed.
  artifacts:
    - pytest suite includes traceability contract tests.
    - pytest suite includes isolation guard tests.
  key_links:
    - tests exercise ai router + retrieval diagnostics contracts affected by Phase 5.
---

<objective>
Add automated validation suite for traceability and isolation guarantees.

Purpose: convert R5/R6 into regression-resistant executable checks.
Output: backend pytest coverage for provenance contracts and isolation guards.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-traceability-and-validation/05-02-PLAN.md
@backend/app/routers/ai.py
@backend/app/routers/stats.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pytest scaffolding for backend API contract tests</name>
  <files>backend/tests/conftest.py</files>
  <read_first>backend/requirements.txt, backend/app/main.py</read_first>
  <action>Create minimal async pytest fixtures for app/client setup and common auth/context stubs required by phase validation tests.</action>
  <verify><automated>pytest backend/tests -q --collect-only</automated></verify>
  <acceptance_criteria>
    - pytest collection succeeds for backend/tests
  </acceptance_criteria>
  <done>Test infrastructure exists for traceability/isolation checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add traceability contract tests</name>
  <files>backend/tests/test_traceability_contract.py</files>
  <read_first>backend/app/routers/ai.py, backend/app/schemas/ai.py</read_first>
  <action>Write tests asserting project-chat and diagnostics responses include required provenance fields and anchor metadata.</action>
  <verify><automated>pytest backend/tests/test_traceability_contract.py -q</automated></verify>
  <acceptance_criteria>
    - tests fail when provenance fields are absent
    - tests pass with expected provenance contract shape
  </acceptance_criteria>
  <done>Traceability output contract is regression-protected by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add isolation guard tests</name>
  <files>backend/tests/test_isolation_guards.py</files>
  <read_first>backend/app/routers/ai.py, backend/app/services/rag_service.py</read_first>
  <action>Write tests for unauthorized project access and scope-denied retrieval behavior to ensure R6 guarantees are automatically enforced.</action>
  <verify><automated>pytest backend/tests/test_isolation_guards.py -q</automated></verify>
  <acceptance_criteria>
    - unauthorized access paths return expected error behavior
    - scope guard regressions are detected by tests
  </acceptance_criteria>
  <done>Isolation guarantees are validated continuously via pytest.
</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest backend/tests -q --collect-only` succeeds
- [ ] traceability tests pass
- [ ] isolation tests pass
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- R5 and R6 are enforced by executable regression tests
  </success_criteria>

<output>
After completion, create `.planning/phases/05-traceability-and-validation/05-03-SUMMARY.md`
</output>

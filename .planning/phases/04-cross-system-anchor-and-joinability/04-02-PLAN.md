---
phase: 04-cross-system-anchor-and-joinability
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/app/routers/articles.py
  - backend/app/services/qdrant_retrieval_service.py
  - backend/app/services/rag_service.py
autonomous: true
requirements:
  - R4
user_setup: []
must_haves:
  truths:
    - Vector payload contract includes canonical `paper_id` and scoped `project_id`.
    - Article write path propagates canonical `paper_id` into vector synchronization calls.
  artifacts:
    - article router sets and persists canonical paperId at create/update boundaries.
    - qdrant adapter uses payload shape with `paper_id` and project filter metadata.
  key_links:
    - rag retrieval responses can surface `paper_id` for downstream join operations.
---

<objective>
Propagate canonical `paper_id` to vector payload boundaries.

Purpose: ensure vector records can be joined to relational records through the same anchor key.
Output: article write propagation + qdrant payload contract alignment.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cross-system-anchor-and-joinability/04-01-PLAN.md
@backend/app/routers/articles.py
@backend/app/services/qdrant_retrieval_service.py
@backend/app/services/rag_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce canonical paperId in article create/update flow</name>
  <files>backend/app/routers/articles.py</files>
  <read_first>backend/app/routers/articles.py, backend/app/services/anchor_service.py</read_first>
  <action>Integrate anchor service in article create/update endpoints so `paperId` is normalized/generated and persisted consistently.</action>
  <verify><automated>python -m compileall backend/app/routers/articles.py</automated></verify>
  <acceptance_criteria>
    - create/update routes assign canonical paperId before commit
    - existing clients continue working when paperId is omitted
  </acceptance_criteria>
  <done>Relational write boundary guarantees canonical paper anchor values.</done>
</task>

<task type="auto">
  <name>Task 2: Add paper_id payload contract in qdrant adapter</name>
  <files>backend/app/services/qdrant_retrieval_service.py</files>
  <read_first>backend/app/services/qdrant_retrieval_service.py</read_first>
  <action>Add explicit payload builder/validation requiring `paper_id` and `project_id`; ensure search and upsert contracts preserve both fields.</action>
  <verify><automated>python -m compileall backend/app/services/qdrant_retrieval_service.py</automated></verify>
  <acceptance_criteria>
    - adapter rejects payloads missing paper_id
    - payload contract documents mandatory paper_id and project_id
  </acceptance_criteria>
  <done>Vector boundary is anchor-aware and scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Surface paper_id in RAG retrieval metadata</name>
  <files>backend/app/services/rag_service.py</files>
  <read_first>backend/app/services/rag_service.py</read_first>
  <action>Include `paper_id` in retrieved article metadata so downstream graph/sql/vector joins can use the same anchor in runtime flows.</action>
  <verify><automated>python -m compileall backend/app/services/rag_service.py</automated></verify>
  <acceptance_criteria>
    - retrieval metadata includes paper_id when available
  </acceptance_criteria>
  <done>Join key is visible in retrieval outputs for cross-system workflows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Article writes consistently produce canonical paperId
- [ ] Qdrant payload contracts require paper_id + project_id
- [ ] RAG output includes paper_id anchor metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Vector and relational anchor propagation is aligned
  </success_criteria>

<output>
After completion, create `.planning/phases/04-cross-system-anchor-and-joinability/04-02-SUMMARY.md`
</output>

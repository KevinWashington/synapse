---
phase: 04-cross-system-anchor-and-joinability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/article.py
  - backend/app/schemas/article.py
  - backend/app/database.py
  - backend/app/services/anchor_service.py
autonomous: true
requirements:
  - R4
user_setup: []
must_haves:
  truths:
    - PostgreSQL article records persist a canonical `paper_id` field.
    - Canonicalization/generation rules for `paper_id` are centralized.
  artifacts:
    - Article model contains `paperId` column with project-aware lookup constraints.
    - Anchor service provides deterministic `paper_id` normalize/generate helpers.
  key_links:
    - Article schema and DB migration/backfill flow align on same canonical key rules.
---

<objective>
Establish canonical `paper_id` contract on relational records.

Purpose: create a stable cross-system anchor source-of-truth in PostgreSQL.
Output: model/schema/database anchor foundation plus normalization service.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cross-system-anchor-and-joinability/CONTEXT.md
@backend/app/models/article.py
@backend/app/schemas/article.py
@backend/app/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical paperId field to Article model</name>
  <files>backend/app/models/article.py, backend/app/database.py</files>
  <read_first>backend/app/models/article.py, backend/app/database.py</read_first>
  <action>Add `paperId` column to Article model and startup migration/backfill logic so existing rows gain canonical anchors before downstream joins rely on them.</action>
  <verify><automated>python -m compileall backend/app/models/article.py backend/app/database.py</automated></verify>
  <acceptance_criteria>
    - model includes `paperId`
    - migration/backfill path exists for missing paperId values
  </acceptance_criteria>
  <done>Relational layer can persist canonical paper anchor per article.</done>
</task>

<task type="auto">
  <name>Task 2: Create anchor normalization service</name>
  <files>backend/app/services/anchor_service.py</files>
  <read_first>backend/app/models/article.py</read_first>
  <action>Create service that normalizes provided anchors and deterministically generates `paper_id` from DOI/title/year fallback when missing.</action>
  <verify><automated>python -m compileall backend/app/services/anchor_service.py</automated></verify>
  <acceptance_criteria>
    - service exposes normalize and generate helpers
    - helper output is deterministic for same input payload
  </acceptance_criteria>
  <done>Canonical anchor logic is centralized and reusable.</done>
</task>

<task type="auto">
  <name>Task 3: Expose paperId in article schema contracts</name>
  <files>backend/app/schemas/article.py</files>
  <read_first>backend/app/schemas/article.py</read_first>
  <action>Add `paperId` fields to create/update/response schemas so API contracts carry anchor key consistently.</action>
  <verify><automated>python -m compileall backend/app/schemas/article.py</automated></verify>
  <acceptance_criteria>
    - response schema includes `paperId`
    - create/update schema supports optional `paperId` input
  </acceptance_criteria>
  <done>API layer can exchange canonical anchor values explicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Article model and schemas compile with paperId support
- [ ] Anchor service compiles and provides deterministic helpers
- [ ] DB startup path includes paperId backfill/migration logic
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- PostgreSQL side of anchor consistency is established
  </success_criteria>

<output>
After completion, create `.planning/phases/04-cross-system-anchor-and-joinability/04-01-SUMMARY.md`
</output>

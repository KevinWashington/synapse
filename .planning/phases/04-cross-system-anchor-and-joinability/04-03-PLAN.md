---
phase: 04-cross-system-anchor-and-joinability
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - backend/app/services/postgres_mcp_service.py
  - backend/app/routers/stats.py
  - backend/app/services/qdrant_retrieval_service.py
autonomous: true
requirements:
  - R4
user_setup: []
must_haves:
  truths:
    - System can verify joinability by paper_id between PostgreSQL and vector payloads.
    - Operators can inspect anchor consistency health for a project.
  artifacts:
    - postgres MCP service supports lookup by canonical paper_id.
    - diagnostics endpoint reports anchor consistency metrics.
  key_links:
    - diagnostics correlate postgres paper_id counts with vector payload anchor counts.
---

<objective>
Add anchor joinability diagnostics and verification helpers.

Purpose: prove cross-system joins are feasible and observable in operations.
Output: joinability checks and diagnostics output for `paper_id` consistency.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/services/postgres_mcp_service.py
@backend/app/services/qdrant_retrieval_service.py
@backend/app/routers/stats.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postgres lookup by paper_id</name>
  <files>backend/app/services/postgres_mcp_service.py</files>
  <read_first>backend/app/services/postgres_mcp_service.py</read_first>
  <action>Add a constrained helper for relational lookup/counts by `paper_id` and `projectId` to support joinability checks.</action>
  <verify><automated>python -m compileall backend/app/services/postgres_mcp_service.py</automated></verify>
  <acceptance_criteria>
    - service exposes paper_id lookup path for project-scoped checks
  </acceptance_criteria>
  <done>Relational layer can answer anchor existence queries by paper_id.</done>
</task>

<task type="auto">
  <name>Task 2: Add vector-side anchor inspection helper</name>
  <files>backend/app/services/qdrant_retrieval_service.py</files>
  <read_first>backend/app/services/qdrant_retrieval_service.py</read_first>
  <action>Add adapter helper that reports expected anchor fields (`paper_id`, `project_id`) for scoped records and exposes count/availability status.</action>
  <verify><automated>python -m compileall backend/app/services/qdrant_retrieval_service.py</automated></verify>
  <acceptance_criteria>
    - adapter exposes joinability inspection response shape
  </acceptance_criteria>
  <done>Vector layer can report anchor availability for diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Expose anchor consistency diagnostics endpoint</name>
  <files>backend/app/routers/stats.py</files>
  <read_first>backend/app/routers/stats.py</read_first>
  <action>Add stats endpoint that returns paper_id consistency metrics for a project (postgres count, vector count, missing anchor indicators).</action>
  <verify><automated>python -m compileall backend/app/routers/stats.py</automated></verify>
  <acceptance_criteria>
    - diagnostics endpoint exposes anchor consistency fields
  </acceptance_criteria>
  <done>Operators can verify cross-system anchor joinability at runtime.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] paper_id lookup helper exists in postgres MCP service
- [ ] vector inspection helper exposes anchor field status
- [ ] stats diagnostics reports anchor consistency metrics by project
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- R4 joinability is measurable and operationally visible
  </success_criteria>

<output>
After completion, create `.planning/phases/04-cross-system-anchor-and-joinability/04-03-SUMMARY.md`
</output>

---
phase: 02-vector-retrieval-with-isolation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/ai.py
  - backend/app/services/rag_service.py
  - backend/app/models/project.py
autonomous: true
requirements:
  - R2
  - R6
user_setup: []
must_haves:
  truths:
    - Project chat retrieval path enforces both project_id scope and owner authorization.
    - Retrieval requests cannot proceed for projects outside current user ownership.
  artifacts:
    - ai router validates ownership before retrieval call.
    - rag_service scope checks include project_id constraints in all retrieval paths.
  key_links:
    - router passes authenticated user context to retrieval service boundary.
---

<objective>
Harden retrieval isolation in current project-chat path.

Purpose: eliminate cross-project data leakage risk in existing flow.
Output: explicit owner + project scope enforcement in API and retrieval service.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vector-retrieval-with-isolation/CONTEXT.md
@backend/app/routers/ai.py
@backend/app/services/rag_service.py
@backend/app/models/project.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce owner check before retrieval</name>
  <files>backend/app/routers/ai.py</files>
  <read_first>backend/app/routers/ai.py, backend/app/models/project.py</read_first>
  <action>Before RAG retrieval in `/project-chat`, validate that `projectId` belongs to `current_user.id`; return 403 for unauthorized access.</action>
  <verify>Select-String -Path backend/app/routers/ai.py -Pattern "projectId|owner|403|Acesso negado"</verify>
  <acceptance_criteria>
    - unauthorized project access returns 403
    - retrieval not executed when ownership check fails
  </acceptance_criteria>
  <done>API boundary enforces owner scope.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize project scope handling in rag_service</name>
  <files>backend/app/services/rag_service.py</files>
  <read_first>backend/app/services/rag_service.py</read_first>
  <action>Ensure all retrieval branches use explicit project-scoped constraints and do not allow unfiltered fallback queries.</action>
  <verify>Select-String -Path backend/app/services/rag_service.py -Pattern "projectId|WHERE|fallback|scope"</verify>
  <acceptance_criteria>
    - both vector and fallback queries remain project-scoped
  </acceptance_criteria>
  <done>Service-level retrieval is consistently scoped.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Unauthorized user cannot retrieve another project's articles
- [ ] Retrieval still returns scoped results for authorized users
- [ ] No unfiltered query path remains
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Isolation hardening is effective in current path
  </success_criteria>

<output>
After completion, create `.planning/phases/02-vector-retrieval-with-isolation/02-01-SUMMARY.md`
</output>

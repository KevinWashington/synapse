---
phase: 02-vector-retrieval-with-isolation
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/app/services/rag_service.py
  - backend/app/routers/ai.py
  - backend/app/routers/stats.py
autonomous: true
requirements:
  - R2
  - R6
user_setup: []
must_haves:
  truths:
    - Isolation behavior is observable and testable.
    - Diagnostics expose retrieval backend and isolation guard outcomes.
  artifacts:
    - retrieval diagnostics include backend and project scope metadata.
    - negative path checks cover unauthorized access and unscoped retrieval attempts.
  key_links:
    - diagnostics correlate retrieval path with user/project scope checks.
---

<objective>
Add verification and observability for retrieval isolation behavior.

Purpose: make isolation guarantees auditable in operations.
Output: diagnostics and checks that prove scoping is consistently enforced.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-vector-retrieval-with-isolation/02-02-PLAN.md
@backend/app/services/rag_service.py
@backend/app/routers/ai.py
@backend/app/routers/stats.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retrieval diagnostics fields</name>
  <files>backend/app/services/rag_service.py, backend/app/routers/stats.py</files>
  <read_first>backend/app/services/rag_service.py, backend/app/routers/stats.py</read_first>
  <action>Expose active retrieval backend, project scope metadata, and isolation guard counters in diagnostics output.</action>
  <verify>Select-String -Path backend/app/services/rag_service.py,backend/app/routers/stats.py -Pattern "backend|scope|isolation|project"</verify>
  <acceptance_criteria>
    - diagnostics include backend and scope indicators
  </acceptance_criteria>
  <done>Isolation state is visible for operations.</done>
</task>

<task type="auto">
  <name>Task 2: Add negative-path verification script/checks</name>
  <files>backend/app/routers/ai.py</files>
  <read_first>backend/app/routers/ai.py</read_first>
  <action>Add testable checks (or lightweight validation hooks) for unauthorized project access and unscoped retrieval denial.</action>
  <verify>Select-String -Path backend/app/routers/ai.py -Pattern "403|unauthorized|project|scope"</verify>
  <acceptance_criteria>
    - unauthorized path is explicit and testable
  </acceptance_criteria>
  <done>Negative-path behavior is documented in code and verifiable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Diagnostics expose retrieval backend and scope metadata
- [ ] Unauthorized path returns 403 consistently
- [ ] Isolation regressions are detectable via checks
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Isolation guarantees are operationally visible
  </success_criteria>

<output>
After completion, create `.planning/phases/02-vector-retrieval-with-isolation/02-03-SUMMARY.md`
</output>

---
phase: 02-vector-retrieval-with-isolation
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/app/services/qdrant_retrieval_service.py
  - backend/app/config.py
  - backend/app/services/rag_service.py
autonomous: true
requirements:
  - R2
  - R6
user_setup: []
must_haves:
  truths:
    - Qdrant retrieval adapter enforces mandatory project filter in every query.
    - RAG service can call adapter behind a stable interface.
  artifacts:
    - qdrant retrieval service exists with filtered search API.
    - config includes qdrant collection/index defaults.
  key_links:
    - rag_service integrates adapter while preserving response contract.
---

<objective>
Introduce Qdrant retrieval adapter with mandatory project isolation.

Purpose: align retrieval architecture with Hybrid GraphRAG target stack.
Output: adapter-based retrieval path that always injects `project_id` filter.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vector-retrieval-with-isolation/RESEARCH.md
@backend/app/services/rag_service.py
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Qdrant retrieval adapter</name>
  <files>backend/app/services/qdrant_retrieval_service.py</files>
  <read_first>backend/app/services/rag_service.py, backend/app/services/embedding_service.py</read_first>
  <action>Create adapter service that accepts query embedding and project_id, performs filtered search, and rejects calls without project_id.</action>
  <verify>Select-String -Path backend/app/services/qdrant_retrieval_service.py -Pattern "project_id|filter|search|qdrant"</verify>
  <acceptance_criteria>
    - adapter refuses unscoped searches
    - adapter defines explicit filtered search API
  </acceptance_criteria>
  <done>Qdrant adapter enforces filter at tool boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add Qdrant config and feature switch</name>
  <files>backend/app/config.py</files>
  <read_first>backend/app/config.py</read_first>
  <action>Add Qdrant endpoint/collection settings and a retrieval backend toggle for progressive rollout.</action>
  <verify>Select-String -Path backend/app/config.py -Pattern "QDRANT|RETRIEVAL_BACKEND"</verify>
  <acceptance_criteria>
    - config contains Qdrant connectivity and backend toggle
  </acceptance_criteria>
  <done>Runtime can select retrieval backend safely.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate adapter path in rag_service</name>
  <files>backend/app/services/rag_service.py</files>
  <read_first>backend/app/services/rag_service.py, backend/app/services/qdrant_retrieval_service.py</read_first>
  <action>Introduce backend-selection branch that uses Qdrant adapter while preserving current API response format.</action>
  <verify>Select-String -Path backend/app/services/rag_service.py -Pattern "qdrant|RETRIEVAL_BACKEND|project_id"</verify>
  <acceptance_criteria>
    - retrieval can route to qdrant path
    - response schema remains stable for callers
  </acceptance_criteria>
  <done>RAG retrieval supports filtered Qdrant mode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Adapter rejects missing project_id
- [ ] Scoped retrieval works in qdrant mode
- [ ] Existing project-chat response contract remains unchanged
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Qdrant path is isolation-safe and ready for rollout
  </success_criteria>

<output>
After completion, create `.planning/phases/02-vector-retrieval-with-isolation/02-02-SUMMARY.md`
</output>
